import React from "react";
import { Icon } from 'semantic-ui-react';


const Notifications = () => {
    return (
        <div>
            <div> Notifications </div>
            <br></br>
            <h3>backend <Icon name="angle right" />  Notifications via kafka msg bus <Icon name="angle right" /> Kafka-broker(Dockerized) <Icon name="angle left" /> JS or java endpoint with kafka consumer that exposes the selected notifications via an Api <Icon name="angle right" /> redux-thunk <Icon name="angle right" /> react UI</h3 >
        </div>
    )
}

export default Notifications